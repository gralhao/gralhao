{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PHPCS Fix",
            "type": "shell",
            "command": "${workspaceRoot}/vendor/bin/phpcbf",
            "presentation": {
                "reveal": "never"
            },
        },
        {
            "label": "PHPCS Check",
            "type": "shell",
            "command": "${workspaceRoot}/vendor/bin/phpcs",
            "presentation": {
                "reveal": "never"
            },
        }
    ]
}